.row.text-center
  .columns.small-12
    a.button.expand.large(href='/projects/new') New project
    hr
    h1 Projects (
      span(ng-bind='projects.length')
      span )
    hr

.row.text-center(ng-repeat='project in projects')
  .columns.small-12
    h1
      a(ng-href='/projects/{{project._id}}' ng-bind='project.name')

    h2
      small Client: 
      span(ng-bind='project.client')

    button.alert(ng-click='remove(project, $index)') Remove

  .columns.small-12
    hr